package com.jipple.sql.catalyst.expressions;

/**
 * Defines values for `SQLConf` config of fallback mode. Use for test only.
 */
public enum CodegenObjectFactoryMode {
    FALLBACK, CODEGEN_ONLY, NO_CODEGEN;
}
